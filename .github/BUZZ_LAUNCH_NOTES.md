# Buzz launch polish

- Remove the blocking 757 Pulse overlay from the map so the primary interface stays unobstructed.
- Keep the existing map-first desktop experience and current filters/navigation.
- Deduplicate venue records by Google Place ID before ranking, with exact canonical name/coordinates as the safe fallback.
